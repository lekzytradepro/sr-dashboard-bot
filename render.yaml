services:
  - type: web
    name: sr-dashboard-bot
    env: docker
    repo: https://github.com/YOUR_USERNAME/sr-dashboard-bot
    branch: main
    dockerfilePath: Dockerfile
    region: frankfurt
    plan: free
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: ADMIN_ID
        sync: false
      - key: TWELVEDATA_KEY
        sync: false
